export const metadata = {
    title: 'ZfO Admin',
    robots: {
        index: false,
        follow: false,
    },
};

export default function AdminLayout({ children }) {
    return <>{children}</>;
}
